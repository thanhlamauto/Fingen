% WACV 2027 Paper Draft
\documentclass[10pt,twocolumn,letterpaper]{article}

\usepackage[review,datasets]{wacv}

\usepackage{times}
\usepackage{epsfig}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{xcolor}

\definecolor{wacvblue}{rgb}{0.21,0.49,0.74}
\usepackage[pagebackref,breaklinks,colorlinks,allcolors=wacvblue]{hyperref}

\def\wacvPaperID{*****}
\def\confName{WACV}
\def\confYear{2027}

\newcommand{\method}{RidgeLoRA-FP}
\newcommand{\TODO}[1]{\textcolor{red}{[TODO: #1]}}

\title{\method: Lightweight Ridge-Conditioned Fingerprint Diffusion for Affordable Cross-Sensor Synthetic Data}

\author{
Anonymous WACV submission\\
Paper ID *****}

\begin{document}
\maketitle

\begin{abstract}
Training fingerprint classifiers is still bottlenecked by data access: useful
training sets need many impressions per finger, but biometric data is expensive
to collect, difficult to share, and constrained by privacy regulation.  The
problem becomes especially concrete when a lab or company deploys a newly
purchased sensor: only a small calibration set may be available, while
collecting enough labeled impressions to retrain a robust recognizer is slow
and legally sensitive.  We present \method, a lightweight synthetic-data
pipeline for this setting.  Stage 1 samples privacy-preserving virtual
fingerprint identities from an unconditional latent diffusion prior.  Stage 2
renders multiple impressions from each identity under target-sensor appearance
using ridge conditioning, pose-mask alignment, and a small sensor LoRA adapter.
Compared with recent Stable-Diffusion-based fingerprint generators, our active
Stage-2 transfer path uses only 23.39M parameters, reducing the generator
backbone size by 97.3--98.5\%.  This compact design targets low-end GPU
research hardware while preserving explicit ridge control for recognition
utility.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Fingerprint recognition research is constrained by the scarcity of public,
large-scale, multi-impression datasets.  Unlike generic image classification,
the training data must represent both identity-level structure, such as ridge
flow and minutiae configuration, and acquisition-level variation, such as
sensor noise, contrast, pressure, cropping, pose, and background artifacts.
Collecting such data is expensive and privacy-sensitive, and the resulting
datasets are often too small for training robust deep recognizers.
This bottleneck is most visible when a new sensor is introduced.  The sensor
may have a different contrast curve, foreground mask, scratch pattern, or
cropping distribution, but collecting enough repeated impressions from real
users to retrain a classifier is often impractical at the beginning of
deployment.

The same pressure has already made synthetic data an important direction in
face recognition, where rendered and diffusion-generated identities are used to
reduce dependence on web-crawled personal images and consent-limited
collections~\cite{bae2023digiface,kim2023dcface}.  Fingerprints pose a related
but more structural version of the problem: useful synthetic data must preserve
ridge and minutiae identity while producing realistic impression-level changes
under different acquisition devices.

Recent fingerprint generators address this gap by synthesizing multiple
impressions of each synthetic finger.  FPGAN-Control~\cite{shoshan2023fpgan}
is the closest baseline for our setting because it is trained and evaluated on
NIST SD302/N2N and explicitly controls fingerprint appearance.  GenPrint
\cite{grosz2024genprint} and Ridge Scale Aligned
Diffusion~\cite{hsu2026ridge} extend the direction with Stable-Diffusion-based
pipelines, ControlNet-style structure guidance, and image/text style
conditions.  These designs are powerful, but the inherited backbone is large:
the public Stable Diffusion v1 configuration uses an 860M-parameter UNet, and
ControlNet-style inference adds a second large conditional branch.  That level
of compute is not always available to smaller biometrics labs that want to
prototype recognition models on low-end GPUs.

This work explores a more pragmatic point in the design space.  Rather than
fine-tuning a large general-purpose text-to-image model, \method uses a compact
fingerprint-specific latent diffusion model.  Identity generation is deliberately
kept unconditional so that the first stage learns a fingerprint identity prior
without binding sampled ridges to a reference sensor style.  Sensor appearance
is handled after ridge extraction through the Stage-2 renderer and its sensor
adapters.  Identity preservation is handled through an explicit ridge condition
in the second stage.  Finally, sensor adaptation is performed with Stage-2 UNet
LoRA only: the first-stage identity generator and the structural control path
remain frozen.

Our contributions are:
\begin{itemize}
    \item A lightweight two-stage latent diffusion pipeline that separates
    unconditional fingerprint identity generation from ridge-conditioned,
    sensor-specific impression rendering.
    \item A Stage-2 UNet LoRA adaptation framework for transferring the
    generator to new sensors while keeping the identity prior and ridge-control
    path frozen.
\end{itemize}

\section{Related Work}
\label{sec:related}

\paragraph{Synthetic fingerprints.}
Classical systems such as SFinGe~\cite{cappelli2002synthetic} construct
fingerprints using hand-designed ridge flow, shape, and noise models.  Recent
GAN-based methods improve realism and scale.  PrintsGAN~\cite{engelsma2022prints}
generates large sets of synthetic fingers with multiple impressions per finger,
which is essential for training embedding-based recognizers.  FPGAN-Control
\cite{shoshan2023fpgan} further disentangles identity and appearance factors,
allowing image appearance attributes such as sensor and pressure to be varied
while keeping identity fixed.  Because FPGAN-Control is evaluated on the NIST
SD302/N2N setting, we use it as our main competitor.

\paragraph{Fingerprint diffusion.}
Latent diffusion models reduce pixel-space diffusion cost by denoising in a
compressed latent representation~\cite{rombach2022ldm}.  GenPrint
\cite{grosz2024genprint} applies a multimodal latent diffusion model to
fingerprints, using text prompts and image style embeddings to control
acquisition type, sensor, quality, and identity.  GenPrint is a strong
reference point for universality, but it is built on a larger Stable
Diffusion-style backbone and a broad multi-dataset training recipe.  Our goal
is complementary: provide a smaller, faster pipeline that can be trained,
sampled, and adapted by recognition teams with modest hardware.

\paragraph{Structural control and efficient adaptation.}
ControlNet~\cite{zhang2023controlnet} adds trainable control branches to a
frozen diffusion model for spatially aligned conditions.  For fingerprints,
ridge maps are a natural structural condition because they encode the ridge
flow and minutiae layout used by matchers.  LoRA~\cite{hu2022lora} provides a
low-rank adaptation mechanism for fine-tuning large neural networks with few
trainable parameters.  We use LoRA as a practical sensor-adaptation mechanism
by tuning only Stage-2 UNet adapter weights.

\section{Method}
\label{sec:method}

\subsection{Overview}

\method is a two-stage generator.  Stage 1 samples a synthetic fingerprint
identity from an unconditional fingerprint latent prior.  Stage 2 renders
multiple impressions of that identity under target sensor appearances.  Both
stages operate on $512 \times 512$ grayscale images encoded into
$128 \times 128 \times 3$ VQ-VAE latents.  The VQ-VAE is frozen.

Let $x$ be a fingerprint image and $z=E(x)$ its latent.  Stage 1 models
$p_\theta(z)$ and generates a base identity image $\hat{x}^{id}$.  A ridge
extractor produces a control map $r = R(\hat{x}^{id})$.  Stage 2 then samples
impressions $\hat{x}^{imp}_1,\ldots,\hat{x}^{imp}_K$ conditioned on the ridge
map and the selected target-sensor rendering parameters.

\subsection{Unconditional Identity Generation}

Stage 1 is an unconditional latent diffusion model trained on fingerprint
latents.  It does not receive a sensor label, reference image set, or learned
style descriptor.  This choice keeps the identity prior compact and avoids
coupling the sampled ridge identity to a particular acquisition style.  The
generated image is used only as an identity source; its ridge map is extracted
and passed to Stage 2 for sensor-specific rendering.

The Stage-1 diffusion objective is standard noise prediction:
\[
    \mathcal{L}_{\text{stage1}} =
    \mathbb{E}_{z,t,\epsilon}
    \left[\left\|
        \epsilon - \epsilon_\theta(z_t,t)
    \right\|_1\right].
\]

\subsection{Ridge-Conditioned Instance Generation}

Stage 2 receives a ridge image $r$ and target sensor rendering information.  The
ridge map is encoded by a ControlNet-style branch initialized from the Stage-1
UNet where compatible.  The denoising UNet predicts the diffusion noise using
the noisy latent, timestep, and residual control features:
\[
    \epsilon_\phi(z_t,t,r) =
    U_\phi\left(z_t,t,C_\phi(z_t,t,r)\right).
\]
The base Stage-2 training loss is again an L1 noise-prediction objective.
During controlled synthesis, the same identity ridge can be paired with
multiple target-sensor adapters or rendering settings to create cross-sensor
impressions.

\subsection{Stage-2 UNet LoRA for Sensor Adaptation}

For a new sensor, we use the available target-sensor samples only to adapt
appearance.  The VQ-VAE, Stage-1 identity model, ridge extractor, and
ControlNet structural path remain frozen.  We insert LoRA modules into the
Stage-2 denoising UNet projection layers and optimize only these LoRA weights.
The intent is to change sensor texture, contrast, background statistics, and
local artifact style while preserving the behavior of the ridge-control path.
At inference time, the adapter is selected by sensor, while the input ridge and
sampling settings provide identity and appearance control.

\subsection{Pose-Mask Alignment and Control Perturbation}

A direct ridge condition can over-constrain the output pose: all target sensor
instances may inherit the same fingerprint placement as the Stage-1 identity
image.  To avoid this, we construct a per-sensor pose-mask library from real
target-sensor foreground masks.  For each generated impression, we cycle
through the target sensor's pose masks, align the ridge foreground to the mask
using a bounded similarity transform, and keep only the intersection between
the transformed ridge mask and the target pose mask.  We also apply mild
control perturbations: small rotations, translations, rare horizontal flips,
ridge dropout, speckle, and light morphology.  These perturbations provide
intra-class variation while keeping minutiae topology mostly intact.

\section{Experimental Setup}
\label{sec:experiments}

\subsection{Datasets}

\paragraph{NIST SD302A.}
SD302A~\cite{nist302} is the primary benchmark and the only dataset on which we compare
against FPGAN-Control.  Our current SD302A split uses identity-clean finger
labels, where filenames of the form
\texttt{SUBJECT\_SENSOR\_CAPTURE\_FRGP} are mapped to \texttt{SUBJECT\_FRGP}.
The held-out validation split contains 400 finger identities and 2,734 images.

\paragraph{Additional sensor data.}
SD301A~\cite{nist301} and SD302B are used to increase sensor coverage for our generator.
Only 500 ppi images are used when a dataset provides duplicate 500/1000 ppi
versions of the same impression.  FVC 2004~\cite{fvc2004} sensors are used for qualitative
and zero/few-shot sensor-transfer studies.  We do not compare to FPGAN-Control
on these datasets because they are outside the baseline's primary SD302A
setting and constitute the additional contribution of this work.

\subsection{Generation Protocol}

For a synthetic dataset with 500 identities and 10 impressions per identity,
Stage 1 samples identity images unconditionally.  Each identity image is
ridge-extracted and passed to Stage 2.  The 10 impressions are generated across
target sensors A--H using the corresponding target-sensor renderer or adapter.
In the pose-aligned setting, each sensor cycles through a library of up to 32
pose masks.

\begin{table}[t]
    \centering
    \caption{Main generation configuration used in the current SD302A
    synthetic dataset.}
    \label{tab:generation_config}
    \begin{tabular}{ll}
        \toprule
        Item & Setting \\
        \midrule
        Image resolution & $512 \times 512$ grayscale \\
        Latent resolution & $128 \times 128 \times 3$ \\
        Stage-1 condition & none; unconditional latent prior \\
        Stage-1 injection & none \\
        Stage-2 condition & ridge map + target-sensor renderer/adapters \\
        Synthetic set & 500 identities, 10 impressions each \\
        SD302A target sensors & A, B, C, D, E, F, G, H \\
        Pose masks & up to 32 per sensor, cycled \\
        Sampler & DDIM, 50 steps \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Recognition Utility Protocol}

Following the spirit of FPGAN-Control, the main utility metric is whether
synthetic fingerprints can train a fingerprint recognizer that transfers to
real held-out fingerprints.  We train a ResNet50 embedding model with CosFace
supervision and evaluate all genuine and impostor pairs on the SD302A held-out
split.  We report equal error rate (EER) and true accept rate (TAR) at
FAR=$1\%$ and FAR=$0.1\%$.  Train/validation leakage is avoided by splitting
real data by identity and by using synthetic identities that do not appear in
the real validation set.

\subsection{FPGAN-Control Comparison Protocol}

FPGAN-Control is the primary baseline.  The final paper comparison should use
one of two protocols:
\begin{itemize}
    \item \textbf{Paper-number comparison:} report FPGAN-Control's published
    SD302A/N2N recognition-utility numbers and evaluate \method under the
    closest matching synthetic-only and real-plus-synthetic protocols.
    \item \textbf{Reproduced comparison:} generate or obtain FPGAN-Control
    samples with the same number of identities, impressions, and SD302A sensor
    distribution as \method, then train the same recognizer and run the same
    pairwise verification code.
\end{itemize}
The reproduced comparison is preferred because recognizer architecture,
identity split, and label parsing materially affect EER and TAR.

\section{Results and Discussion}
\label{sec:results}

\subsection{SD302A Comparison to FPGAN-Control}

For the main SD302A recognition-utility comparison, each synthetic training set
contains 2,000 identities and 10 impressions per identity, for 20,000 training
images.  Recognition models are trained separately on each dataset and
evaluated on the SD302A test split.  Table~\ref{tab:sd302_tar01_backbones}
follows the Table 3 protocol of Ridge Scale Aligned
Diffusion~\cite{hsu2026ridge}: it reports only TAR at FAR=$0.1\%$ and keeps
the ResNet18, ResNet101, and ViT recognizer backbones.

\begin{table*}[t]
    \centering
    \caption{SD302A recognition results under the Table 3 setting of Ridge
    Scale Aligned Diffusion~\cite{hsu2026ridge}.  Each synthetic dataset
    contains 2,000 identities with 10 impressions per identity.  Values are
    TAR@FAR=$0.1\%$ on the SD302A test split, reported as fractions.}
    \label{tab:sd302_tar01_backbones}
    \begin{tabular}{lccc}
        \toprule
        & \multicolumn{3}{c}{TAR@FAR=$0.1\%$} \\
        \cmidrule(lr){2-4}
        Training dataset & ResNet18 & ResNet101 & ViT \\
        \midrule
        NIST SD 302 (real) & 0.6946 & \textbf{0.7457} & \textbf{0.8008} \\
        FPGAN-Control & 0.7181 & 0.6193 & 0.7875 \\
        PrintsGAN & 0.5421 & 0.5122 & 0.5523 \\
        Ours & \textbf{0.7251} & 0.7271 & 0.7531 \\
        \midrule
        NIST SD 302 + FPGAN-Control & 0.8093 & 0.8719 & 0.8935 \\
        NIST SD 302 + PrintsGAN & 0.7299 & 0.7793 & 0.7688 \\
        NIST SD 302 + Ours & \textbf{0.8575} & \textbf{0.9009} & \textbf{0.9128} \\
        \bottomrule
    \end{tabular}
\end{table*}

\subsection{Qualitative Multi-Sensor Generation}

Figure~\ref{fig:sensor_overview} shows the current SD302A multi-sensor
synthetic dataset.  The generation run produced 5,000 images: 500 identities
with 10 impressions each.  Sensor counts are balanced across A--H.  Pose-mask
alignment successfully used 32 unique pose masks for most sensors; sensor D
currently has fewer usable pose masks in the inspected library and should be
expanded before final generation.

\begin{figure*}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/qc_sensor_overview.png}
    \caption{QC sheet for the current SD302A synthetic dataset.  The dataset
    contains 500 synthetic identities and 10 impressions per identity,
    distributed across SD302A sensors A--H.}
    \label{fig:sensor_overview}
\end{figure*}

\begin{figure*}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/qc_identity_spread_first6.png}
    \caption{Examples of multiple impressions generated from the same
    synthetic identity under different sensor conditions.  Pose-mask alignment
    reduces the single-pose failure mode observed when the raw identity ridge is
    used directly as the control image.}
    \label{fig:identity_spread}
\end{figure*}

\subsection{Cross-Sensor Extension}

The additional datasets are used to test sensor coverage rather than to claim a
direct FPGAN-Control comparison.  SD301A and SD302B increase the diversity of
contact and rolled sensors used for sensor-transfer studies.  FVC 2004 provides
compact benchmarks with sensor styles that differ from SD302A.
Figure~\ref{fig:fvc} shows an inspection sheet used to identify FVC sensors
whose image statistics are close enough to SD302A-style sensors that Stage-2
LoRA adaptation is likely to work.

We also ran a preliminary FVC 2004 DB1 cross-sensor recognition test.  The
Stage-2 UNet LoRA adapter was fitted on DB1\_A, then used to generate 500
synthetic identities with 10 impressions per identity.  We trained the same
CosFace ResNet50 recognizer on DB1\_A real data and/or these synthetic samples,
and evaluated all verification pairs on DB1\_B: 80 images, 10 identities, 280
genuine pairs, and 2,880 impostor pairs.  Table~\ref{tab:fvc_db1_transfer}
shows that synthetic data improves the real-only recognizer under this
cross-sensor protocol, although synthetic-only training still transfers poorly
to real DB1\_B images.

\begin{table*}[t]
    \centering
    \caption{Preliminary FVC 2004 DB1 cross-sensor verification.  Recognizers
    are trained on DB1\_A real data and/or \method samples generated with the
    DB1\_A Stage-2 UNet LoRA adapter, then evaluated on all DB1\_B verification
    pairs.  Values are percentages.}
    \label{tab:fvc_db1_transfer}
    \begin{tabular}{lccc}
        \toprule
        Training set & EER $\downarrow$ & TAR@FAR1\% $\uparrow$ & TAR@FAR0.1\% $\uparrow$ \\
        \midrule
        Real only & 28.17 & 6.07 & 1.43 \\
        Synthetic only, 500$\times$10 & 43.57 & 6.43 & 0.36 \\
        Real + synthetic, 500$\times$10 & 25.00 & 11.07 & 5.36 \\
        \bottomrule
    \end{tabular}
\end{table*}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fvc_vs_sd302a_sensor_comparison.png}
    \caption{Qualitative sensor inspection between FVC and SD302A samples.  We
    use these sheets to select plausible unseen sensors for Stage-2 UNet LoRA
    adaptation.}
    \label{fig:fvc}
\end{figure}

\subsection{Ablation Study: First-Stage Latent Resolution}

We use a relatively high-resolution $128 \times 128 \times 3$ VQ-VAE latent
instead of a conventional $32 \times 32$ LDM-style latent.  This choice is
important for fingerprints because ridge flow and minutiae are local,
high-frequency structures.  To isolate the effect of latent spatial
resolution, we keep the same trained VQ-VAE decoder and compare normal
decoding against a $32 \times 32$ bottleneck proxy: the encoded latent is
downsampled from $128 \times 128$ to $32 \times 32$, then upsampled back before
decoding.  This does not introduce a separately trained weaker autoencoder;
it directly tests how much ridge information is lost when the diffusion latent
is forced through a low spatial bottleneck.

Table~\ref{tab:latent_resolution_ablation} and
Figure~\ref{fig:latent_resolution_ablation} show that the $128 \times 128$
VQ-VAE preserves fingerprint ridges almost exactly under a two-pixel matching
tolerance, while the $32 \times 32$ bottleneck removes most ridge detail and
turns minutiae-bearing structures into broad intensity blobs.  This supports
using the larger VQ-VAE latent despite the extra diffusion cost: Stage 1 must
produce a ridge identity that can later be extracted and controlled by Stage 2.

\begin{table*}[t]
    \centering
    \small
    \caption{First-stage latent-resolution ablation on four SD302A examples.
    The $32 \times 32$ variant is a latent-bottleneck proxy using the same
    trained decoder.  Ridge F1 uses ridge maps extracted from real and
    reconstructed images with a two-pixel tolerance.}
    \label{tab:latent_resolution_ablation}
    \begin{tabular}{lcccc}
        \toprule
        First stage & MAE $\downarrow$ & Grad. corr. $\uparrow$ & Ridge F1 $\uparrow$ & Ridge px. ratio \\
        \midrule
        VQ-VAE $128^2$ & 0.055 & 0.895 & 0.994 & 1.05 \\
        Latent bottleneck $32^2$ & 0.224 & -0.058 & 0.219 & 0.11 \\
        \bottomrule
    \end{tabular}
\end{table*}

\begin{figure*}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/ablation_vqvae128_vs_latent32.png}
    \caption{Qualitative first-stage latent-resolution ablation.  Black pixels
    in the diff columns are matched ridges, red pixels are real ridges missed by
    the reconstruction, and green pixels are extra reconstructed ridges.  The
    $128 \times 128$ VQ-VAE latent retains ridge topology, whereas the
    $32 \times 32$ bottleneck proxy loses the local ridge pattern needed for
    fingerprint identity.}
    \label{fig:latent_resolution_ablation}
\end{figure*}

\subsection{Lightweight Design}

The key engineering goal is to make fingerprint synthesis usable by research
groups without large GPU clusters.  Our current Stage-2 TPU training run uses a
compact 64-channel latent UNet and reaches about 7.05 steps/s for global batch
size 256 on TPU v5p-8 after pre-encoding latents, with stable compiled-step
time.  For deployment and sensor adaptation, the important point is that a new
sensor does not require retraining the full generator: only a small Stage-2
UNet LoRA adapter is optimized.  Table~\ref{tab:efficiency} summarizes the
current parameter accounting.  The full two-stage generator, including the
shared VQ-VAE decoder, has 53.58M parameters.  The active Stage-2 transfer path
has 23.39M parameters, which is 97.3\% smaller than the Stable Diffusion v1
UNet alone and about 98.5\% smaller than an SD1.5+ControlNet-scale inference
stack.  For a new sensor, only 12.3K LoRA parameters are trained.

\begin{table}[t]
    \centering
    \caption{Parameter and throughput accounting for the current \method
    implementation.  Parameter counts are measured from local checkpoints.}
    \label{tab:efficiency}
    \begin{tabular}{p{0.66\linewidth}r}
        \toprule
        Component & Parameters \\
        \midrule
        Stage-1 identity UNet & 16.33M \\
        Stage-2 denoising UNet & 16.35M \\
        Stage-2 ControlNet & 7.04M \\
        VQ-VAE encoder/decoder/codebook & 13.86M \\
        Full two-stage generator & 53.58M \\
        Active Stage-2 transfer path & 23.39M \\
        Trainable LoRA per new sensor & 12.3K \\
        \midrule
        Logged Stage-2 generation throughput & 1.05 s/image \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[t]
    \centering
    \caption{Positioning relative to closely related methods.}
    \label{tab:positioning}
    \begin{tabular}{p{0.22\linewidth}p{0.32\linewidth}p{0.34\linewidth}}
        \toprule
        Method & Strength & Limitation addressed by \method \\
        \midrule
        FPGAN-Control & Strong SD302A controllable baseline & We add lightweight diffusion, explicit ridge rendering, and LoRA sensor adapters. \\
        GenPrint & Broad multimodal diffusion and zero-shot style control & We target smaller models and cheaper adaptation for labs with low-end GPUs. \\
        \method & Compact latent diffusion with ridge control & Current recognizer-transfer gap remains the main open issue. \\
        \bottomrule
    \end{tabular}
\end{table}

\section{Limitations}
\label{sec:limitations}

The current synthetic dataset is visually plausible but does not yet transfer
as strongly to real SD302A recognition as desired.  Our internal experiments
suggest that the bottleneck is not only Stage-2 ridge following: the generated
Stage-1 identity ridge may still differ from the real SD302A ridge/minutiae
manifold, and ten impressions generated from one synthetic ridge can be too
structurally similar for recognizer training.  The final report should include
additional minutiae statistics, matcher-based genuine/impostor distributions,
and a reproduced FPGAN-Control comparison under the same recognizer protocol.

\section{Conclusion}
\label{sec:conclusion}

\method is a lightweight fingerprint diffusion pipeline designed for practical
synthetic data generation and sensor adaptation.  It combines a compact
unconditional latent identity model, ridge-conditioned instance generation,
pose-mask alignment, and Stage-2 UNet LoRA adapters for unseen sensors.  The
method is positioned against FPGAN-Control on SD302A, while
SD301A, SD302B, and FVC-style sensors define the cross-sensor extension.  The
remaining work is to finalize the FPGAN-Control comparison, benchmark low-end GPU
sampling and LoRA fine-tuning cost, and close the observed recognizer-transfer
gap with stronger ridge/minutiae realism metrics.

{
    \small
    \bibliographystyle{ieeenat_fullname}
    \bibliography{main}
}

\end{document}
